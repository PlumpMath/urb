(require System)

# TODO:
#
# 1. remove static-class, defstatic, override.
# 2. replace by attr and args.
#
#

(class ReplEnv
	(attr :public :partial :static)
	(progn
		(defun square:Int32
			(attr :public :static)
			(args x:Int32)
			(progn
				(return 
					(* x x))))))